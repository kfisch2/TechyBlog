<article class='post'>
  <h4><a href='/post/{{id}}'>{{title}}</a></h4>
  <h6>{{post_text}}</h6>
  -{{user.username}}
  ({{format_date created_at}})

  {{comments.length}}
    {{format_plural 'comment' comments.length}}

</article>