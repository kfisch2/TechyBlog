{{> post-info post}}

{{#if loggedIn}}
<form class="comment-form">
  <div>
    <input name="comment-body" placeholder="Leave a comment..."></input>
  </div>
  <div>
    <button type="submit" class="btn">add comment</button>
  </div>
</form>
{{/if}}

{{> comments post.comments }}

{{#if loggedIn}}
<script src="/js/comment.js"></script>
{{/if}}